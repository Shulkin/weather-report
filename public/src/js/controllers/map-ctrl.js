angular.module("map.ctrl", ["openlayers-directive"])
.controller("MapCtrl", [function() {
  // === Variables ===
  var vm = this;
  // === Private ===
  // constructor
  function init() {
  }
  // === Start module ===
  init();
  // === Public ===
}]);
